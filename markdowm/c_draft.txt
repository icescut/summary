宏定义，预定义宏，条件编译，#/##，sizeof，typeof，
不初始化，不会有默认值。数组会默认初始化。
整数计算返回整数。
没有string类型，使用char[]，以\0结束。
Iostream,cmath
类型，操作符
&*p,*&p,*p++
数组指针（运算）
函数指针
全局变量
按值传递，按引用传递
*(a+1)+1等于&a[1][1],*(a+1)=>a[1],a[1]在二为数组相当于指向一为数组的指针。
指针数组与二维数组指针变量。
指向指针的指针变量。
